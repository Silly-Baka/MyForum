<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="MyForum.mapper.PostDetailMapper">
    <sql id="allColumn">
        user_id,title,content,type,status,create_time,comment_count,score
    </sql>
    <resultMap id="PostDetailMap" type="MyForum.pojo.PostDetail">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="title" column="title"/>
        <result property="content" column="content"/>
        <result property="type" column="type"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
        <result property="score" column="score"/>
        <result property="commentCount" column="comment_count"/>
    </resultMap>
    <select id="getPostList" resultMap="PostDetailMap">
        select <include refid="allColumn"/>
        from post_detail
        where status != 2
        order by type desc
        limit #{currentPage},#{pageSize}
    </select>
    <select id="getCount" resultType="integer">
        select count(*)
        from post_detail
    </select>
    <select id="getHotPosts" resultMap="PostDetailMap">
        select <include refid="allColumn"/>
        from post_detail
        where status != 2
        order by comment_count and score desc
        limit #{currentPage},#{pageSize}
    </select>
</mapper>